!!!WARNING!!!
===

Do not open the Sample App from here but open rather the `InAppSettingsKit.xcworkspace` in the Root Folder.

You then have to select the InAppSettingsKitSampleApp Scheme and run on your prefered device/simulator.

The project won't compile otherwise.